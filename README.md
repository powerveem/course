# Teacher Landing (React + Vite)

## Run

```
npm install
npm run dev
```

Replace image URLs in `src/ui/data.ts` arrays: `galleryImages`, `homeworkImages`, `reviewsImages`. Update links in `contactLinks` and CTAs.

# course
# course
# course
